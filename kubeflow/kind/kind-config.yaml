kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: worldbank
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    extraPortMappings:
      # KFP UI (via NodePort/ingress if exposed); convenience only
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
  - role: worker
  - role: worker
